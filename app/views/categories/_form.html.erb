<%= simple_form_for(@category) do |f| %>
  <%= f.input :name %>
  <%= f.input :current,
        label: 'Is this a current topic set?' %>
  <%= f.input :colour,
        as: :color %>
  <%= f.button :submit, class: "btn btn-primary" %>
<% end %>
