<%= simple_form_for(@student) do |f| %>
  <%= f.input :name_en,
        label: 'English name',
        error: 'This field is required - put the Chinese name here if necessary' %>
  <%= f.input :name_wechat,
        label: 'WeChat name' %>
  <%= f.input :timezone,
        as: :time_zone,
        label: "Time zone",
        priority: [ ActiveSupport::TimeZone["Beijing"], ActiveSupport::TimeZone["Madrid"], ActiveSupport::TimeZone["London"] ] %>
  <%= f.input :app,
        label: 'What app do they want to use for classes?',
        placeholder: 'eg: WeChat (video or voice-only), Zoom, etc.' %>
  <% if current_user.admin? %>
    <%= f.association :users,
          as: :check_boxes,
          label: 'Teacher(s)' %>
  <% end %>
  <%= f.input :onboarding,
        label: 'Student details',
        placeholder: 'eg: Where are they from? Where do they live? Do they study or work? What is their previous English experience? What is their learning goal?',
        error: 'This field is required' %>
  <%= f.input :notes,
        label: 'Teacher notes',
        placeholder: 'eg: Frequent mistakes they make. Topics to cover/follow up on in future lessons.' %>
  <%= f.button :submit, class: "btn btn-primary" %>
<% end %>
